@model Postagem

<div class="mx-auto w-75">
    <div class="mb-4">
        <h2>Criar uma nova postagem</h2>
    </div>

    <form asp-action="Cadastrar">
        <div class="row mb-3">
            <div class="col-3">
                <label class="form-label">Tag</label>
                <select asp-for="TagId" asp-items="@ViewBag.Tags" class="form-select" required>
                    <option value="">Selecione</option>
                </select>
            </div>
            <div class="col">
                <label asp-for="Titulo" class="form-label"></label>
                <input asp-for="Titulo" class="form-control" placeholder="Título da minha postagem" required>
            </div>
        </div>
        <div class="mb-3">
            <label asp-for="Texto" class="form-label"></label>
            <textarea asp-for="Texto" class="form-control" rows="3" required></textarea>
        </div>
        <div class="mt-3">
            <input type="button" class="btn btn-secondary" value="Voltar" asp-action="Index" />
            <input type="submit" class="btn btn-primary" value="Postar" />
        </div>
    </form>
</div>